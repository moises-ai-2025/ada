import { Plugin } from 'vite';
export interface OptimizeCssModuleOptions {
    dictionary?: string;
    apply?: 'build' | 'serve';
}
export declare const optimizeCssModules: (options?: OptimizeCssModuleOptions) => Plugin;
